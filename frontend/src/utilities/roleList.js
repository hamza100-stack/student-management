export const rolesMap = {
    "Content Developer": "content_developer",
    Student: "student",
    Teacher: "teacher",
    Admin: "admin",
    Block: "block",
};

// Optional: keys list for UI rendering
export const rolesList = Object.keys(rolesMap); // ["Content Developer", "Student", ...]
